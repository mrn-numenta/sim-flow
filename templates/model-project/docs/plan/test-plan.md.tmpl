# Test Plan

## Overview

Summarize the verification scope, major risks, and overall plan.

## Testbench

Describe the planned UVM-lite structure DM3b will implement.

### Sequencers

- `<name>`: payload type, stimulus class, purpose

### Drivers

- `<name>`: target port, protocol, purpose

### Monitors

- `<name>`: observed port or signal, purpose

### Scoreboards

- `<name>`: invariant checked, monitor inputs, comparison strategy

### SimEnvBuilder Wiring

Describe how the complete `SimEnv` will be assembled.

### References

List the modeling-guide chapters, framework docs, and worked examples this plan
expects DM3b to mirror.

## Smoke

- [ ] `<test_name>` -- `<purpose>`; pass criteria: `<specific measurable criteria>`; traces to: `<spec / target / operation>`

## Edge

- [ ] `<test_name>` -- `<purpose>`; pass criteria: `<specific measurable criteria>`; traces to: `<spec / target / operation>`

## Stress

- [ ] `<test_name>` -- `<purpose>`; pass criteria: `<specific measurable criteria>`; traces to: `<spec / target / operation>`

## Random

- [ ] `<test_name>_seed_<N>` -- `<purpose>`; pass criteria: `<specific measurable criteria>`; traces to: `<spec / target / operation>`

## Coverage

- Tool:
- Run command:
- Threshold:
- Report path:

### Exclusions

- `<file or module>`: `<reason>`

## Traceability

### Spec Requirements To Tests

| Requirement | Test(s) |
| --- | --- |
| `<spec requirement>` | `<test names>` |

### Targets To Tests

| Target | Test(s) |
| --- | --- |
| `<target>` | `<test names>` |

### Operations To Tests

| Operation | Test(s) |
| --- | --- |
| `<operation>` | `<test names>` |

## Open Questions

- `<question>`
