[client]
name = "claude"

[claude]
model = "sonnet"
allowed_tools = ["Bash", "Read", "Edit", "Write", "Glob", "Grep"]

[codex]
sandbox = "workspace-write"
approval = "never"

[copilot]
mode = "autopilot"
