Write the critique as JSON to
`docs/critiques/{{ step_id }}-critique.json`. The orchestrator
renders a human-readable `docs/critiques/{{ step_id }}-critique.md`
from that JSON automatically; do NOT write the markdown yourself.

{{ critique_json_schema }}
