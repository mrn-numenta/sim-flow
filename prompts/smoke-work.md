# Smoke Work Session

This file is a placeholder used by the Phase 1 smoke integration test.
The real step instruction files are authored in Phase 3 (DM) and Phase 6
(DS). The mock client bypasses the prompt content entirely and applies
fixture files from disk, so this text is not evaluated by an LLM.
